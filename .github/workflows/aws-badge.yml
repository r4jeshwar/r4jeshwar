name: Update badges

on:
  schedule:
    - cron: "1 * * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main
        with:       
          CREDLY_USER: rajeshwarrajakumar2000@gmail.com # optional, but default will use the same from github
